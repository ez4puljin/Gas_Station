{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @fuel/types build && pnpm --filter @fuel/schemas build && pnpm --filter @fuel/web build",
  "outputDirectory": "apps/web/.next",
  "github": { "silent": true }
}

-- ГҮЙЦЭТГЭЛ: борлуулалтын тайлан огнооны мужаар + `station_id IN (...)`-аар шүүдэг.
-- Байгаа `sale_station_id_sold_at_idx` (композит) нь олон салбарын IN-д сонгогддоггүй тул
-- planner бүтэн хүснэгтийг seq scan хийдэг байсан. Огноогоор дангаар индексжүүлнэ.
-- Хэмжилт (99,000 борлуулалт): 7 хоногийн муж 56ms -> 8ms.
CREATE INDEX "sale_sold_at_idx" ON "sale"("sold_at");
